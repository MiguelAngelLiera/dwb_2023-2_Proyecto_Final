server:
  port: 8888
  
spring:
  cloud:
    config:
      server: 
        git:
          uri: https://ivan_saavedra@bitbucket.org/ivan_saavedra/config-data.git
          search-paths: config-data/api
          default-label: "master"
          
  security:
    user:
      name: root
      password: adminadmin
 
